services:
  - type: web
    name: telegram-saas-bot
    env: docker
    rootDir: telegram-saas
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
